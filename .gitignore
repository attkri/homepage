# Lokale Arbeitsdaten, die nicht öffentlich oder versioniert werden sollen
.logs/
.todo/
.opencode/
.tmp/
.secrets/
.Secrets/
project.code-workspace
.Analyses/**

# Hugo-Publish-Output wird in CI als Artifact deployed, nicht versioniert
docs/
resources/_gen/

# Interne Agenten-Doku, nicht fuer die oeffentliche Website bestimmt
AGENTS.md

# Temporaere Hugo-Sperrdatei waehrend lokaler Builds
.hugo_build.lock
