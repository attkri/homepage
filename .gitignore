# Lokale OpenCode-Arbeitsdaten (nicht Teil der Website-Quellen)
.opencode/

# Enthält personenbezogene/private Daten, bleibt strikt lokal
.Secrets/

# Hugo-Publish-Output wird in CI als Artifact deployed, nicht versioniert
docs/

# Interne Agenten-Doku, nicht fuer die oeffentliche Website bestimmt
AGENTS.md

# Generierter Hugo-Cache, erzeugt nur Git-Rauschen
resources/_gen/

# Temporaere Hugo-Sperrdatei waehrend lokaler Builds
.hugo_build.lock

# Lokale VSCode-Arbeitsbereichsdatei, nicht fuer die Versionskontrolle bestimmt
project.code-workspace